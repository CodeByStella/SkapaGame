using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartButFalse : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Buttons.Startb = false;
    }
}
